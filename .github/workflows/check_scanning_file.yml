# .github/workflows/check_scanning_file.yml
name: Check Scanning File Upload

on:
  push:
    paths:
      - 'conversation/csv/scanning.xlsx' # Triggers only when scanning.xlsx is pushed

jobs:
  check_and_mark:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Mark scanning.xlsx as uploaded
        run: |
          mkdir -p conversation/Temp # Ensure directory exists
          TEMP_FILE="conversation/Temp/upload_temp.txt"
          TIMESTAMP=$(date +"%Y%m%d_%H%M%S") # Get current timestamp

          # Read existing content, update or add 'scanning_uploaded' with timestamp
          if [ -f "$TEMP_FILE" ]; then
            CONTENT=$(cat "$TEMP_FILE")
          else
            CONTENT=""
          fi
          
          # Remove any existing scanning_uploaded line to avoid duplicates
          CLEANED_CONTENT=$(echo "$CONTENT" | grep -v "scanning_uploaded:")
          
          # Add the new scanning_uploaded line with timestamp
          NEW_CONTENT="${CLEANED_CONTENT}\nscanning_uploaded:true_${TIMESTAMP}"
          
          # Remove leading/trailing newlines and write to file
          echo -e "$NEW_CONTENT" | sed '/^$/d' > "$TEMP_FILE"
          
          echo "Scanning file status marked in $TEMP_FILE with timestamp."

      - name: Commit upload_temp.txt changes
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add conversation/Temp/upload_temp.txt
          git commit -m "Marked scanning.xlsx as uploaded with timestamp." || echo "No changes to commit for scanning.xlsx status."
          git push
